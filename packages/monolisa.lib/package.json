{
  "name": "monolisa.lib",
  "version": "1.0.0",
  "main": "dist",
  "types": "index",
  "scripts": {
    "build": "yarn run babel . -d --out-dir dist --extensions '.ts'",
    "test": "echo skipping for now"
  }
}